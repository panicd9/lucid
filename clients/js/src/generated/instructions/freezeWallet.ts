/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LUCID_PROGRAM_ADDRESS } from "../programs";

export const FREEZE_WALLET_DISCRIMINATOR = 4;

export function getFreezeWalletDiscriminatorBytes() {
  return getU8Encoder().encode(FREEZE_WALLET_DISCRIMINATOR);
}

export type FreezeWalletInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountMetaIntent extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountMetaIntent extends string
        ? ReadonlyAccount<TAccountMetaIntent>
        : TAccountMetaIntent,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type FreezeWalletInstructionData = { discriminator: number };

export type FreezeWalletInstructionDataArgs = {};

export function getFreezeWalletInstructionDataEncoder(): FixedSizeEncoder<FreezeWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: FREEZE_WALLET_DISCRIMINATOR }),
  );
}

export function getFreezeWalletInstructionDataDecoder(): FixedSizeDecoder<FreezeWalletInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getFreezeWalletInstructionDataCodec(): FixedSizeCodec<
  FreezeWalletInstructionDataArgs,
  FreezeWalletInstructionData
> {
  return combineCodec(
    getFreezeWalletInstructionDataEncoder(),
    getFreezeWalletInstructionDataDecoder(),
  );
}

export type FreezeWalletInput<
  TAccountWallet extends string = string,
  TAccountMetaIntent extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Any meta-intent PDA (for approver verification) */
  metaIntent: Address<TAccountMetaIntent>;
  /** Approver */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getFreezeWalletInstruction<
  TAccountWallet extends string,
  TAccountMetaIntent extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: FreezeWalletInput<
    TAccountWallet,
    TAccountMetaIntent,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): FreezeWalletInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountMetaIntent,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: true },
    metaIntent: { value: input.metaIntent ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("metaIntent", accounts.metaIntent),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getFreezeWalletInstructionDataEncoder().encode({}),
    programAddress,
  } as FreezeWalletInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountMetaIntent,
    TAccountAuthority
  >);
}

export type ParsedFreezeWalletInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet PDA */
    wallet: TAccountMetas[0];
    /** Any meta-intent PDA (for approver verification) */
    metaIntent: TAccountMetas[1];
    /** Approver */
    authority: TAccountMetas[2];
  };
  data: FreezeWalletInstructionData;
};

export function parseFreezeWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFreezeWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      metaIntent: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getFreezeWalletInstructionDataDecoder().decode(instruction.data),
  };
}
