/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LUCID_PROGRAM_ADDRESS } from "../programs";

export const ADD_INTENTS_BATCH_DISCRIMINATOR = 2;

export function getAddIntentsBatchDiscriminatorBytes() {
  return getU8Encoder().encode(ADD_INTENTS_BATCH_DISCRIMINATOR);
}

export type AddIntentsBatchInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddIntentsBatchInstructionData = { discriminator: number };

export type AddIntentsBatchInstructionDataArgs = {};

export function getAddIntentsBatchInstructionDataEncoder(): FixedSizeEncoder<AddIntentsBatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: ADD_INTENTS_BATCH_DISCRIMINATOR }),
  );
}

export function getAddIntentsBatchInstructionDataDecoder(): FixedSizeDecoder<AddIntentsBatchInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getAddIntentsBatchInstructionDataCodec(): FixedSizeCodec<
  AddIntentsBatchInstructionDataArgs,
  AddIntentsBatchInstructionData
> {
  return combineCodec(
    getAddIntentsBatchInstructionDataEncoder(),
    getAddIntentsBatchInstructionDataDecoder(),
  );
}

export type AddIntentsBatchInput<
  TAccountWallet extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Rent payer */
  payer: TransactionSigner<TAccountPayer>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddIntentsBatchInstruction<
  TAccountWallet extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: AddIntentsBatchInput<
    TAccountWallet,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddIntentsBatchInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddIntentsBatchInstructionDataEncoder().encode({}),
    programAddress,
  } as AddIntentsBatchInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedAddIntentsBatchInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet PDA */
    wallet: TAccountMetas[0];
    /** Rent payer */
    payer: TAccountMetas[1];
    /** System program */
    systemProgram: TAccountMetas[2];
  };
  data: AddIntentsBatchInstructionData;
};

export function parseAddIntentsBatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddIntentsBatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddIntentsBatchInstructionDataDecoder().decode(instruction.data),
  };
}
