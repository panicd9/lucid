/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LUCID_PROGRAM_ADDRESS } from "../programs";

export const DEACTIVATE_INTENT_DISCRIMINATOR = 3;

export function getDeactivateIntentDiscriminatorBytes() {
  return getU8Encoder().encode(DEACTIVATE_INTENT_DISCRIMINATOR);
}

export type DeactivateIntentInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type DeactivateIntentInstructionData = { discriminator: number };

export type DeactivateIntentInstructionDataArgs = {};

export function getDeactivateIntentInstructionDataEncoder(): FixedSizeEncoder<DeactivateIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: DEACTIVATE_INTENT_DISCRIMINATOR }),
  );
}

export function getDeactivateIntentInstructionDataDecoder(): FixedSizeDecoder<DeactivateIntentInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getDeactivateIntentInstructionDataCodec(): FixedSizeCodec<
  DeactivateIntentInstructionDataArgs,
  DeactivateIntentInstructionData
> {
  return combineCodec(
    getDeactivateIntentInstructionDataEncoder(),
    getDeactivateIntentInstructionDataDecoder(),
  );
}

export type DeactivateIntentInput<
  TAccountWallet extends string = string,
  TAccountIntent extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Intent PDA to deactivate */
  intent: Address<TAccountIntent>;
  /** Approver of the intent */
  authority: TransactionSigner<TAccountAuthority>;
};

export function getDeactivateIntentInstruction<
  TAccountWallet extends string,
  TAccountIntent extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: DeactivateIntentInput<
    TAccountWallet,
    TAccountIntent,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): DeactivateIntentInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountIntent,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: false },
    intent: { value: input.intent ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("intent", accounts.intent),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getDeactivateIntentInstructionDataEncoder().encode({}),
    programAddress,
  } as DeactivateIntentInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountIntent,
    TAccountAuthority
  >);
}

export type ParsedDeactivateIntentInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet PDA */
    wallet: TAccountMetas[0];
    /** Intent PDA to deactivate */
    intent: TAccountMetas[1];
    /** Approver of the intent */
    authority: TAccountMetas[2];
  };
  data: DeactivateIntentInstructionData;
};

export function parseDeactivateIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeactivateIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      intent: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getDeactivateIntentInstructionDataDecoder().decode(instruction.data),
  };
}
