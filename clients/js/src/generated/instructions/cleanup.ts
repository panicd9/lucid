/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { LUCID_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLEANUP_DISCRIMINATOR = 30;

export function getCleanupDiscriminatorBytes() {
  return getU8Encoder().encode(CLEANUP_DISCRIMINATOR);
}

export type CleanupInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountRentRefund extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountRentRefund extends string
        ? WritableAccount<TAccountRentRefund>
        : TAccountRentRefund,
      ...TRemainingAccounts,
    ]
  >;

export type CleanupInstructionData = { discriminator: number };

export type CleanupInstructionDataArgs = {};

export function getCleanupInstructionDataEncoder(): FixedSizeEncoder<CleanupInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CLEANUP_DISCRIMINATOR }),
  );
}

export function getCleanupInstructionDataDecoder(): FixedSizeDecoder<CleanupInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCleanupInstructionDataCodec(): FixedSizeCodec<
  CleanupInstructionDataArgs,
  CleanupInstructionData
> {
  return combineCodec(
    getCleanupInstructionDataEncoder(),
    getCleanupInstructionDataDecoder(),
  );
}

export type CleanupInput<
  TAccountProposal extends string = string,
  TAccountIntent extends string = string,
  TAccountRentRefund extends string = string,
> = {
  /** Proposal PDA to close */
  proposal: Address<TAccountProposal>;
  /** Intent PDA (decrement active count) */
  intent: Address<TAccountIntent>;
  /** Recipient of rent refund */
  rentRefund: Address<TAccountRentRefund>;
};

export function getCleanupInstruction<
  TAccountProposal extends string,
  TAccountIntent extends string,
  TAccountRentRefund extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: CleanupInput<TAccountProposal, TAccountIntent, TAccountRentRefund>,
  config?: { programAddress?: TProgramAddress },
): CleanupInstruction<
  TProgramAddress,
  TAccountProposal,
  TAccountIntent,
  TAccountRentRefund
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    proposal: { value: input.proposal ?? null, isWritable: true },
    intent: { value: input.intent ?? null, isWritable: true },
    rentRefund: { value: input.rentRefund ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.proposal),
      getAccountMeta(accounts.intent),
      getAccountMeta(accounts.rentRefund),
    ],
    data: getCleanupInstructionDataEncoder().encode({}),
    programAddress,
  } as CleanupInstruction<
    TProgramAddress,
    TAccountProposal,
    TAccountIntent,
    TAccountRentRefund
  >);
}

export type ParsedCleanupInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Proposal PDA to close */
    proposal: TAccountMetas[0];
    /** Intent PDA (decrement active count) */
    intent: TAccountMetas[1];
    /** Recipient of rent refund */
    rentRefund: TAccountMetas[2];
  };
  data: CleanupInstructionData;
};

export function parseCleanupInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCleanupInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      proposal: getNextAccount(),
      intent: getNextAccount(),
      rentRefund: getNextAccount(),
    },
    data: getCleanupInstructionDataDecoder().decode(instruction.data),
  };
}
