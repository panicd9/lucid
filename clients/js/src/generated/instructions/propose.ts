/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { LUCID_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PROPOSE_DISCRIMINATOR = 10;

export function getProposeDiscriminatorBytes() {
  return getU8Encoder().encode(PROPOSE_DISCRIMINATOR);
}

export type ProposeInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeInstructionData = { discriminator: number };

export type ProposeInstructionDataArgs = {};

export function getProposeInstructionDataEncoder(): FixedSizeEncoder<ProposeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: PROPOSE_DISCRIMINATOR }),
  );
}

export function getProposeInstructionDataDecoder(): FixedSizeDecoder<ProposeInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getProposeInstructionDataCodec(): FixedSizeCodec<
  ProposeInstructionDataArgs,
  ProposeInstructionData
> {
  return combineCodec(
    getProposeInstructionDataEncoder(),
    getProposeInstructionDataDecoder(),
  );
}

export type ProposeInput<
  TAccountWallet extends string = string,
  TAccountIntent extends string = string,
  TAccountProposal extends string = string,
  TAccountInstructionsSysvar extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Intent PDA */
  intent: Address<TAccountIntent>;
  /** Proposal PDA to create */
  proposal: Address<TAccountProposal>;
  /** Instructions sysvar (for Ed25519 verification) */
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  /** Rent payer */
  payer: TransactionSigner<TAccountPayer>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getProposeInstruction<
  TAccountWallet extends string,
  TAccountIntent extends string,
  TAccountProposal extends string,
  TAccountInstructionsSysvar extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: ProposeInput<
    TAccountWallet,
    TAccountIntent,
    TAccountProposal,
    TAccountInstructionsSysvar,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountIntent,
  TAccountProposal,
  TAccountInstructionsSysvar,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: false },
    intent: { value: input.intent ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.intent),
      getAccountMeta(accounts.proposal),
      getAccountMeta(accounts.instructionsSysvar),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposeInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountIntent,
    TAccountProposal,
    TAccountInstructionsSysvar,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedProposeInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet PDA */
    wallet: TAccountMetas[0];
    /** Intent PDA */
    intent: TAccountMetas[1];
    /** Proposal PDA to create */
    proposal: TAccountMetas[2];
    /** Instructions sysvar (for Ed25519 verification) */
    instructionsSysvar: TAccountMetas[3];
    /** Rent payer */
    payer: TAccountMetas[4];
    /** System program */
    systemProgram: TAccountMetas[5];
  };
  data: ProposeInstructionData;
};

export function parseProposeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      intent: getNextAccount(),
      proposal: getNextAccount(),
      instructionsSysvar: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeInstructionDataDecoder().decode(instruction.data),
  };
}
