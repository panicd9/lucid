/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LUCID_PROGRAM_ADDRESS } from "../programs";

export const ADD_INTENT_DISCRIMINATOR = 1;

export function getAddIntentDiscriminatorBytes() {
  return getU8Encoder().encode(ADD_INTENT_DISCRIMINATOR);
}

export type AddIntentInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddIntentInstructionData = { discriminator: number };

export type AddIntentInstructionDataArgs = {};

export function getAddIntentInstructionDataEncoder(): FixedSizeEncoder<AddIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: ADD_INTENT_DISCRIMINATOR }),
  );
}

export function getAddIntentInstructionDataDecoder(): FixedSizeDecoder<AddIntentInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getAddIntentInstructionDataCodec(): FixedSizeCodec<
  AddIntentInstructionDataArgs,
  AddIntentInstructionData
> {
  return combineCodec(
    getAddIntentInstructionDataEncoder(),
    getAddIntentInstructionDataDecoder(),
  );
}

export type AddIntentInput<
  TAccountWallet extends string = string,
  TAccountIntent extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Intent PDA to create */
  intent: Address<TAccountIntent>;
  /** Rent payer */
  payer: TransactionSigner<TAccountPayer>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddIntentInstruction<
  TAccountWallet extends string,
  TAccountIntent extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LUCID_PROGRAM_ADDRESS,
>(
  input: AddIntentInput<
    TAccountWallet,
    TAccountIntent,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddIntentInstruction<
  TProgramAddress,
  TAccountWallet,
  TAccountIntent,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUCID_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: true },
    intent: { value: input.intent ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("intent", accounts.intent),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddIntentInstructionDataEncoder().encode({}),
    programAddress,
  } as AddIntentInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountIntent,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedAddIntentInstruction<
  TProgram extends string = typeof LUCID_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet PDA */
    wallet: TAccountMetas[0];
    /** Intent PDA to create */
    intent: TAccountMetas[1];
    /** Rent payer */
    payer: TAccountMetas[2];
    /** System program */
    systemProgram: TAccountMetas[3];
  };
  data: AddIntentInstructionData;
};

export function parseAddIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      wallet: getNextAccount(),
      intent: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddIntentInstructionDataDecoder().decode(instruction.data),
  };
}
