//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLEANUP_DISCRIMINATOR: u8 = 30;

/// Accounts.
#[derive(Debug)]
pub struct Cleanup {
    /// Proposal PDA to close
    pub proposal: solana_address::Address,
    /// Intent PDA (decrement active count)
    pub intent: solana_address::Address,
    /// Recipient of rent refund
    pub rent_refund: solana_address::Address,
}

impl Cleanup {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.proposal, false));
        accounts.push(solana_instruction::AccountMeta::new(self.intent, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.rent_refund,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = CleanupInstructionData::new().try_to_vec().unwrap();

        solana_instruction::Instruction {
            program_id: crate::LUCID_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CleanupInstructionData {
    discriminator: u8,
}

impl CleanupInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 30 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CleanupInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `Cleanup`.
///
/// ### Accounts:
///
///   0. `[writable]` proposal
///   1. `[writable]` intent
///   2. `[writable]` rent_refund
#[derive(Clone, Debug, Default)]
pub struct CleanupBuilder {
    proposal: Option<solana_address::Address>,
    intent: Option<solana_address::Address>,
    rent_refund: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CleanupBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Proposal PDA to close
    #[inline(always)]
    pub fn proposal(&mut self, proposal: solana_address::Address) -> &mut Self {
        self.proposal = Some(proposal);
        self
    }
    /// Intent PDA (decrement active count)
    #[inline(always)]
    pub fn intent(&mut self, intent: solana_address::Address) -> &mut Self {
        self.intent = Some(intent);
        self
    }
    /// Recipient of rent refund
    #[inline(always)]
    pub fn rent_refund(&mut self, rent_refund: solana_address::Address) -> &mut Self {
        self.rent_refund = Some(rent_refund);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = Cleanup {
            proposal: self.proposal.expect("proposal is not set"),
            intent: self.intent.expect("intent is not set"),
            rent_refund: self.rent_refund.expect("rent_refund is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `cleanup` CPI accounts.
pub struct CleanupCpiAccounts<'a, 'b> {
    /// Proposal PDA to close
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// Intent PDA (decrement active count)
    pub intent: &'b solana_account_info::AccountInfo<'a>,
    /// Recipient of rent refund
    pub rent_refund: &'b solana_account_info::AccountInfo<'a>,
}

/// `cleanup` CPI instruction.
pub struct CleanupCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Proposal PDA to close
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// Intent PDA (decrement active count)
    pub intent: &'b solana_account_info::AccountInfo<'a>,
    /// Recipient of rent refund
    pub rent_refund: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> CleanupCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CleanupCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            proposal: accounts.proposal,
            intent: accounts.intent,
            rent_refund: accounts.rent_refund,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.proposal.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.intent.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.rent_refund.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = CleanupInstructionData::new().try_to_vec().unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::LUCID_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.proposal.clone());
        account_infos.push(self.intent.clone());
        account_infos.push(self.rent_refund.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `Cleanup` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` proposal
///   1. `[writable]` intent
///   2. `[writable]` rent_refund
#[derive(Clone, Debug)]
pub struct CleanupCpiBuilder<'a, 'b> {
    instruction: Box<CleanupCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CleanupCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CleanupCpiBuilderInstruction {
            __program: program,
            proposal: None,
            intent: None,
            rent_refund: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Proposal PDA to close
    #[inline(always)]
    pub fn proposal(&mut self, proposal: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.proposal = Some(proposal);
        self
    }
    /// Intent PDA (decrement active count)
    #[inline(always)]
    pub fn intent(&mut self, intent: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.intent = Some(intent);
        self
    }
    /// Recipient of rent refund
    #[inline(always)]
    pub fn rent_refund(
        &mut self,
        rent_refund: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.rent_refund = Some(rent_refund);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CleanupCpi {
            __program: self.instruction.__program,

            proposal: self.instruction.proposal.expect("proposal is not set"),

            intent: self.instruction.intent.expect("intent is not set"),

            rent_refund: self
                .instruction
                .rent_refund
                .expect("rent_refund is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CleanupCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    proposal: Option<&'b solana_account_info::AccountInfo<'a>>,
    intent: Option<&'b solana_account_info::AccountInfo<'a>>,
    rent_refund: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
